# Application Environment
# Options: development, production
APP_ENV=development

# Debug Mode (shows detailed errors in development)
# Options: true, false
APP_DEBUG=true

# Application Name (used in templates)
APP_NAME="Twig Starter Template"

# Base URL (optional, for generating absolute URLs)
# APP_URL=http://localhost:8000
