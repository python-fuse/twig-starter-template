{
  "name": "ucodes/twig-starter-template",
  "description": "This is a starter template built with love by uCodes to speed up twig development.",
  "type": "project",
  "require": {
    "twig/twig": "^3.0"
  },
  "license": "MIT",
  "autoload": {
    "psr-4": {
      "Ucodes\\TwigStarterTemplate\\": "src/"
    }
  },
  "scripts": {
    "build:css": [
      "@php -r \"if (!file_exists('node_modules')) { echo 'Run npm install first'; exit(1); }\"",
      "npm run build:css"
    ],
    "post-install-cmd": [
      "@php -r \"echo 'Run npm install && npm run build:css to compile Tailwind CSS'.PHP_EOL;\""
    ]
  },
  "authors": [
    {
      "name": "ucodes",
      "email": "ummuktar917@gmail.com"
    }
  ]
}
